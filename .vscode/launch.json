{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Docker Environment",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/run.cmd",
      "console": "integratedTerminal"
    }
  ]
}
